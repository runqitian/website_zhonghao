server:
  port: 8080

spring:
  datasource:
    username: root
    password: trq371402
    url: jdbc:mysql://localhost:3306/zhonghaollc?useUnicode=true&characterEncoding=utf-8&useSSL=true&serverTimezone=UTC
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis:
  mapper-locations: classpath:mapper/*Mapper.xml
  type-aliases-package: com.zhonghaollc.zhonghaosite.entity

#showSql
logging:
  level:
    com:
      example:
        mapper : debug